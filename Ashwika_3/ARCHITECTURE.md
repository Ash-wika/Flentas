# Architecture Diagram - High Availability + Auto Scaling

## 🏗️ Complete Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              INTERNET                                    │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                                 │ HTTP/HTTPS
                                 │
                    ┌────────────▼────────────┐
                    │   Internet Gateway      │
                    └────────────┬────────────┘
                                 │
        ┌────────────────────────┴────────────────────────┐
        │                    VPC                          │
        │                 10.0.0.0/16                     │
        │                                                 │
        │  ┌──────────────────────────────────────────┐  │
        │  │         PUBLIC SUBNETS                   │  │
        │  │  ┌────────────────┬────────────────┐     │  │
        │  │  │  AZ-1          │  AZ-2          │     │  │
        │  │  │  10.0.0.0/24   │  10.0.1.0/24   │     │  │
        │  │  │                │                │     │  │
        │  │  │  ┌──────────────────────────┐   │     │  │
        │  │  │  │ Application Load Balancer│   │     │  │
        │  │  │  │  (Internet-Facing)       │   │     │  │
        │  │  │  │  - Health Checks         │   │     │  │
        │  │  │  │  - Cross-Zone LB         │   │     │  │
        │  │  │  └──────────┬───────────────┘   │     │  │
        │  │  │             │                   │     │  │
        │  │  │  ┌──────────┴───────────┐       │     │  │
        │  │  │  │ NAT GW   │  NAT GW  │       │     │  │
        │  │  │  │ (EIP-1)  │  (EIP-2) │       │     │  │
        │  │  │  └──────────┬───────────┘       │     │  │
        │  │  └─────────────┼───────────────────┘     │  │
        │  └────────────────┼─────────────────────────┘  │
        │                   │                            │
        │  ┌────────────────┼─────────────────────────┐  │
        │  │         PRIVATE SUBNETS                  │  │
        │  │  ┌─────────────▼──────┬─────────────┐    │  │
        │  │  │  AZ-1              │  AZ-2       │    │  │
        │  │  │  10.0.10.0/24      │ 10.0.11.0/24│    │  │
        │  │  │                    │             │    │  │
        │  │  │  ┌──────────────────────────┐    │    │  │
        │  │  │  │  Auto Scaling Group      │    │    │  │
        │  │  │  │  Min: 2, Max: 4          │    │    │  │
        │  │  │  └──────────┬───────────────┘    │    │  │
        │  │  │             │                    │    │  │
        │  │  │  ┌──────────▼──────┐  ┌─────────▼──┐ │  │
        │  │  │  │ EC2 Instance 1  │  │EC2 Instance│ │  │
        │  │  │  │ - Nginx         │  │2           │ │  │
        │  │  │  │ - Resume Site   │  │- Nginx     │ │  │
        │  │  │  │ - Encrypted EBS │  │- Resume    │ │  │
        │  │  │  │ - IMDSv2        │  │- Encrypted │ │  │
        │  │  │  └─────────────────┘  └────────────┘ │  │
        │  │  └──────────────────────────────────────┘  │
        │  └─────────────────────────────────────────────┘
        └─────────────────────────────────────────────────┘
```

## 📊 Traffic Flow

### Inbound Traffic (User Request)
```
User Browser
    ↓
Internet
    ↓
Internet Gateway
    ↓
Application Load Balancer (Public Subnet)
    ↓
Target Group (Health Check)
    ↓
EC2 Instance (Private Subnet)
    ↓
Nginx Web Server
    ↓
Resume Website (HTML/CSS/JS)
    ↓
Response back to user
```

### Outbound Traffic (Instance Updates)
```
EC2 Instance (Private Subnet)
    ↓
NAT Gateway (Public Subnet)
    ↓
Internet Gateway
    ↓
Internet (yum updates, etc.)
```

## 🔒 Security Layers

```
Layer 1: Internet Gateway
         ↓ (Public traffic only)
Layer 2: ALB Security Group
         - Allow: HTTP (80), HTTPS (443) from 0.0.0.0/0
         - Deny: All other inbound
         ↓
Layer 3: Application Load Balancer
         - Health checks
         - SSL termination (if configured)
         ↓
Layer 4: EC2 Security Group
         - Allow: HTTP (80) from ALB only
         - Allow: SSH (22) from your IP only
         - Deny: All other inbound
         ↓
Layer 5: Private Subnet
         - No direct internet access
         - Outbound via NAT Gateway only
         ↓
Layer 6: Instance-level Security
         - IMDSv2 enforced
         - Encrypted EBS
         - CloudWatch monitoring
```

## 🔄 Auto Scaling Flow

```
┌─────────────────────────────────────────────────────────┐
│              CloudWatch Metrics                          │
│  - CPU Utilization                                      │
│  - ALB Request Count Per Target                         │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│         Auto Scaling Policies                           │
│  Policy 1: CPU > 70% → Scale Out                       │
│  Policy 2: Requests > 1000/target → Scale Out          │
│  Policy 3: CPU < 70% → Scale In                        │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│         Auto Scaling Group Actions                      │
│  - Launch new instances (if scaling out)                │
│  - Terminate instances (if scaling in)                  │
│  - Register with Target Group                           │
│  - Wait for health checks                               │
└─────────────────────────────────────────────────────────┘
```

## 🌐 Network Routing

### Public Route Table
```
Destination         Target
10.0.0.0/16        local
0.0.0.0/0          igw-xxxxx (Internet Gateway)
```

### Private Route Table (AZ-1)
```
Destination         Target
10.0.0.0/16        local
0.0.0.0/0          nat-xxxxx1 (NAT Gateway 1)
```

### Private Route Table (AZ-2)
```
Destination         Target
10.0.0.0/16        local
0.0.0.0/0          nat-xxxxx2 (NAT Gateway 2)
```

## 🎯 Load Balancer Configuration

```
┌─────────────────────────────────────────────────────────┐
│         Application Load Balancer                       │
├─────────────────────────────────────────────────────────┤
│  Listener: HTTP:80                                      │
│  ├─ Default Action: Forward to Target Group            │
│  └─ Rules: None (can add path-based routing)           │
├─────────────────────────────────────────────────────────┤
│  Target Group: ha-resume-tg                            │
│  ├─ Protocol: HTTP                                      │
│  ├─ Port: 80                                            │
│  ├─ Health Check:                                       │
│  │   ├─ Path: /                                         │
│  │   ├─ Interval: 30s                                   │
│  │   ├─ Timeout: 5s                                     │
│  │   ├─ Healthy Threshold: 2                            │
│  │   └─ Unhealthy Threshold: 2                          │
│  └─ Targets: Auto Scaling Group instances              │
└─────────────────────────────────────────────────────────┘
```

## 🚀 Launch Template

```
┌─────────────────────────────────────────────────────────┐
│         Launch Template                                  │
├─────────────────────────────────────────────────────────┤
│  AMI: Amazon Linux 2023 (latest)                        │
│  Instance Type: t2.micro                                │
│  Security Group: ha-resume-ec2-sg                       │
│  User Data: user-data.sh (Nginx + Resume)              │
│  Monitoring: Detailed CloudWatch                        │
│  Metadata: IMDSv2 required                              │
│  EBS:                                                    │
│    ├─ Size: 30GB                                        │
│    ├─ Type: gp3                                         │
│    ├─ Encrypted: Yes                                    │
│    └─ Delete on Termination: Yes                        │
└─────────────────────────────────────────────────────────┘
```

## 📈 Scaling Scenarios

### Scenario 1: Normal Load
```
Time: 10:00 AM
CPU: 30%
Requests: 500/target
Instances: 2 (desired)
Status: ✅ Healthy
```

### Scenario 2: High Load
```
Time: 2:00 PM
CPU: 80%
Requests: 1500/target
Action: Scale Out
Instances: 2 → 3 → 4
Status: ⚡ Scaling
```

### Scenario 3: Low Load
```
Time: 11:00 PM
CPU: 20%
Requests: 200/target
Action: Scale In
Instances: 4 → 3 → 2
Status: 📉 Scaling Down
```

## 🔧 Component Relationships

```
VPC
 ├── Internet Gateway
 ├── Public Subnets (2)
 │    ├── Application Load Balancer
 │    └── NAT Gateways (2)
 ├── Private Subnets (2)
 │    └── Auto Scaling Group
 │         └── EC2 Instances (2-4)
 ├── Route Tables (3)
 │    ├── Public RT
 │    └── Private RTs (2)
 └── Security Groups (2)
      ├── ALB SG
      └── EC2 SG
```

## 💾 Data Flow - Instance Launch

```
1. ASG Decision
   ↓
2. Launch Template
   ↓
3. EC2 Instance Created
   ↓
4. User Data Executes
   ├─ Install Nginx
   ├─ Deploy Website
   └─ Start Services
   ↓
5. Instance Registers with Target Group
   ↓
6. Health Checks Begin
   ├─ Check 1 (30s): Initializing
   ├─ Check 2 (60s): Healthy ✅
   └─ Check 3 (90s): Healthy ✅
   ↓
7. Instance In Service
   ↓
8. Receives Traffic from ALB
```

## 🌍 Multi-AZ Deployment

```
Region: us-east-1
├── AZ-1 (us-east-1a)
│   ├── Public Subnet: 10.0.0.0/24
│   │   ├── ALB (active)
│   │   └── NAT Gateway 1
│   └── Private Subnet: 10.0.10.0/24
│       └── EC2 Instance 1
│
└── AZ-2 (us-east-1b)
    ├── Public Subnet: 10.0.1.0/24
    │   ├── ALB (active)
    │   └── NAT Gateway 2
    └── Private Subnet: 10.0.11.0/24
        └── EC2 Instance 2
```

## 🎯 High Availability Features

```
┌─────────────────────────────────────────────────────────┐
│              Failure Scenarios                           │
├─────────────────────────────────────────────────────────┤
│  Scenario 1: Instance Failure                           │
│  ├─ Health Check Fails                                  │
│  ├─ Instance Marked Unhealthy                           │
│  ├─ ASG Launches Replacement                            │
│  └─ Traffic Routed to Healthy Instances                 │
├─────────────────────────────────────────────────────────┤
│  Scenario 2: AZ Failure                                 │
│  ├─ All instances in AZ-1 fail                          │
│  ├─ ALB routes to AZ-2 instances                        │
│  ├─ ASG launches new instances in AZ-2                  │
│  └─ Service continues with minimal impact               │
├─────────────────────────────────────────────────────────┤
│  Scenario 3: High Traffic                               │
│  ├─ CPU/Requests exceed threshold                       │
│  ├─ Auto Scaling triggers                               │
│  ├─ New instances launched                              │
│  └─ Load distributed across more instances              │
└─────────────────────────────────────────────────────────┘
```

---

**Legend:**
- ┌─┐ : Component/Service
- │   : Connection/Flow
- ▼   : Direction of flow
- ✅  : Healthy/Active
- ⚡  : Scaling action
- 📉  : Scaling down
